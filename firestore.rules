rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Só permite ler e escrever quem estiver autenticado (login feito no painel).
    match /employees/{employeeId} {
      allow read, write: if request.auth != null;
    }
  }
}
